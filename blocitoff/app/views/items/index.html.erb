<h1>All Items</h1>
<% @items.each do |item| %>
  <p><%= link_to item.name%></p>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <%= link_to item.name, post %>
      </h4>
    </div>
  </div>
<% end %>
